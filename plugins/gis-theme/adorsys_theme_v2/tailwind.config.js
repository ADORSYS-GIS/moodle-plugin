// Tailwind CSS v4 config - content and theme are now in CSS (@source and @theme)
// DaisyUI temporarily disabled to test v4 compatibility

const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
export default config;