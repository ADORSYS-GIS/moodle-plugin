moodleDatabaseType: "pgsql"
moodleDatabaseHost: moodle-postgresql-rw  # ✅ service from CNPG cluster
moodleDatabaseName: moodledb              # ✅ must match `initdb.database`
moodleDatabaseUser: moodle
moodleDatabasePassword: moodlepass        # ✅ must match user secret

moodleUsername: user
moodlePassword: bitnami123
moodleEmail: user@example.com

postgresql:
  enabled: false  # ✅ because you're not using Bitnami PostgreSQL chart
